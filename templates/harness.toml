parent = "codex"
# parent = "grok"
# parent = "opencode"
# parent = "omp"
# parent = "pi"
# parent = "agy"

[routing]
# Smart is also the default when this section is omitted. Legacy restores the old ladder.
mode = "smart"

[workers]
codex = false
grok = true
claude = true
cursor = false
opencode = false
omp = false
pi = false
agy = false

[queue]
max_running = 3
# max_per_worker = 2
# [queue.workers]
# grok = 2
